{
  "name": "@pet-genius/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n vite,api,tauri -c cyan,magenta,yellow \"vite\" \"tsx watch ../demo/server.ts\" \"tauri dev\"",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no desktop tests' && exit 0"
  },
  "dependencies": {
    "@pet-genius/widget": "workspace:*",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "vite": "^6.0.5",
    "vite-plugin-solid": "^2.11.0"
  }
}
